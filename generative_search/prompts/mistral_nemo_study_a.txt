You are selecting creators for a project.
Return structured JSON only.
Project query: {query}

Creator profiles:
{profiles}

Return exactly {output_k} unique creators ranked from most suitable to least suitable.
Use only the supplied creator_id and document_id values.
Each reason must be concise and grounded only in the supplied profile.
The document_id must belong to the recommended creator_id.
Return an object with a recommendations array. Each item must include rank, creator_id, document_id, and reason.
Return JSON only.
